import { Client } from "discord.js";
import { Config } from "../models/Config";

export const stopCurrentGame = async (client: Client, config: Config) => {
    // TODO: Stop currently running gamge
};
